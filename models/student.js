function Student(id, name, price, desc, screen, type, frontCamera, backCamera, image) {
  this.id = id;
  this.name = name;
  this.price = price;
  this.desc = desc;
  this.screen = screen;
  this.type = type;
  this.frontCamera = frontCamera;
  this.backCamera = backCamera;
  this.image = image;
  
}

// nodejs java golang python